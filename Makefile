# This repo constains a node script that generates the README.md from repos.txt by querying github

# Prerequisite: set env ACCESS_TOKEN via https://github.com/settings/tokens
default:
	node ./code/main.js